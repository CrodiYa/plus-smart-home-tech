spring:
  output:
    ansi:
      enabled: ALWAYS
  datasource:
    driverClassName: "org.postgresql.Driver"
    url: "jdbc:postgresql://0.0.0.0:5432/smarthome"
    username: "smarthome"
    password: "smarthome"
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql